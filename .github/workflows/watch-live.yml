name: watch-live

# Canli izleyici: kitabin KENDI canli akisindan biten maclari kaydeder.
#
# NEDEN: Her sonuc kaynagi bugune kadar baskasinindi ve her biri tek bir dilimi
# kapsiyordu — football-data futbolu, EuroLeague basketbolu, Setka ise sadece Setka Cup'i.
# Masa tenisi kartinin %58'i (Pro League, TT-Cup, Masters) hicbir arsivde yoktu; voleybol,
# snooker, dart, hentbol ve futsalin ise hic kaynagi yoktu. Aday kaynaklarin bir kismi
# robots.txt'te bizim ajan adimiza kapali ve bu degismeyecek.
#
# Kitap bu maclarin hepsini kendisi oynatiyor ve skoru oynanirken yayinliyor. Bu is akisi
# LiveFeed'i tarar, her fikstürün son skorunu hatirlar ve mac bittiginde (ya akis "Match
# finished" dedigi icin ya da fikstür akistan dustugu icin) sonucu data/results/ altina
# yazar. tools/collect_live.py neyi kaydetmeyi REDDETTIGINI aciklar.
#
# CADENCE OLCULDU, TAHMIN EDILMEDI. 15 dakika boyunca 51 saniyede bir tarayip biten
# maclarin akista NE KADAR KALDIGINA baktim: 274 fikstürün 36'si dustu ve biten bir mac
# akista ~50-150 saniye duruyor. Yani yakalama sansi kabaca (kalma suresi / tarama
# araligi): dakikada bir tarama neredeyse hepsini, saatte bir taramaysa ~%2'sini yakalar.
# Kapsam tarama SURESIYLE orantili, tarama SAYISIYLA degil.
#
# Bu yuzden saat basi 55 dakikalik pencere aciliyor — gunde ~22 saat kesintisiz izleme.
# Onceki hali gunde 4 x 10 dakikaydi ve tek nedeni deponun private olmasiydi: aylik 2.000
# dakikalik kota gunde ~40 dakikadan fazlasina izin vermiyordu. Depo public olunca kisit
# kalkti ve dogru cadans da degisti: 40 dakika gunde ~120 sonuc demekti, 22 saat ise
# birkac bin. Voleybolun 400 sonucluk kalibrasyon esigi aylardan gunlere indi.
#
# Saat basi ayri is, tek uzun is degil: durum her taramada diske, ama repoya ancak is
# bitince yaziliyor, o yuzden bir cokme en fazla bir saat kaybettiriyor.

on:
  schedule:
    - cron: "4 * * * *"
  workflow_dispatch:
    inputs:
      minutes:
        description: "Pencere uzunlugu, dakika (0 = tek tarama)"
        required: false
        default: "55"
      every:
        description: "Taramalar arasi saniye (olculen kalma suresi ~50-150 sn)"
        required: false
        default: "60"
      sports:
        description: "Sadece bu spor idleri (virgullu), bos = izleme listesinin tamami"
        required: false
        default: ""

permissions:
  contents: write

# Ayni anda iki tarama calisirsa ikisi de ayni fikstürü 'kayboldu' sanabilir ve durum
# dosyasi birbirinin uzerine yazar.
concurrency:
  group: watch-live
  cancel-in-progress: false

jobs:
  watch:
    runs-on: ubuntu-latest
    # Bir sonraki saatin isi baslamadan bitmeli, yoksa concurrency kuyrugu buyur.
    timeout-minutes: 57
    steps:
      - uses: actions/checkout@v4

      - name: Sweep the live feed
        run: |
          python3 tools/collect_live.py \
            --minutes "${{ github.event.inputs.minutes || '55' }}" \
            --every "${{ github.event.inputs.every || '60' }}" \
            --sports "${{ github.event.inputs.sports || '' }}"

      # Modeller sadece yeni sonuc geldiginde yeniden kuruluyor. Bir spor ancak kendi
      # tutulan kalibrasyonu gecerse kabul edilir; gecmezse build_generic_model nedenini
      # soyler ve gunluk kosu bunu kapsam raporunda tekrarlar.
      - name: Rebuild the models the sweep touched
        run: |
          CHANGED=$(git diff --name-only -- data/results | sed -n 's#data/results/\([0-9]*\)\.jsonl#\1#p')
          if [ -z "$CHANGED" ]; then
            echo "yeni sonuc yok — model kurulmadi"
            exit 0
          fi
          for SPORT in $CHANGED; do
            python3 tools/build_generic_model.py --sport "$SPORT" \
              || echo "spor $SPORT icin model kurulamadi"
          done

      - name: Commit
        run: |
          set -e
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/results data/models data/live_state.json || true
          if git diff --cached --quiet; then
            echo "degisiklik yok"
            exit 0
          fi
          # Kendi satirlarimizin bir kopyasi. Cakisma olursa bunlari uzak duruma YENIDEN
          # ISLEYECEGIZ: izlenmis bir sonuc tekrar izlenemez, mac bitti. "Bir sonraki
          # calistirma yakalar" burada dogru degil — bir sonraki kosu temiz bir kaptan
          # origin/main ile basliyor, gonderilemeyen satirlar kayboluyor.
          mkdir -p /tmp/bwsave && cp -r data/results /tmp/bwsave/
          git commit -m "results: live sweep $(date -u +%Y-%m-%dT%H:%MZ)"
          for i in 1 2 3; do
            if git pull --rebase origin main && git push; then exit 0; fi
            git rebase --abort 2>/dev/null || true
            git fetch origin main
            git reset --hard origin/main
            python3 tools/merge_store.py /tmp/bwsave/results
            CHANGED=$(git diff --name-only -- data/results \
              | sed -n 's#data/results/\([0-9]*\)\.jsonl#\1#p')
            for SPORT in $CHANGED; do
              python3 tools/build_generic_model.py --sport "$SPORT" >/dev/null 2>&1 || true
            done
            git add data/results data/models || true
            if git diff --cached --quiet; then
              echo "uzak durumda zaten var"
              exit 0
            fi
            git commit -m "results: live sweep $(date -u +%Y-%m-%dT%H:%MZ) (yeniden islendi)"
            sleep $((i * 5))
          done
          echo "push basarisiz — satirlar /tmp/bwsave altinda, bu kap silinince gider"
